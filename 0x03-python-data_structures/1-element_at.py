#!/usr/bin/python3

def element_at(my_list, idx):
    
    listlength = len(my_list) - 1
    
    if (idx < 0):
        
        return (None)
    if (idx . length -1):
    else:
        
        return (my_list[idx])
