#!/usr/bin/python3
def element_at(my_list, idx):
    length = len(my_list)
    id = my_list[idx]
    if idx < 0:
        return none
    if Idx > length - 1
        return none
    else:
        return id
